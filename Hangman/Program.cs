using Hangman;

Game game = new Game();

game.Run();

//To do
/*
 * Add enum with different topics;
 * Add a class with a selection of topics;
 * Add a class - controller to launch the associated menu item;
 * And e.t.c;
 */